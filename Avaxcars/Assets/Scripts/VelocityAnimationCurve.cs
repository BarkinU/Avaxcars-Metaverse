using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VelocityAnimationCurve : MonoBehaviour
{
    /*
        public AnimationCurve carSpeedGraph;
        private float carSpeed;
        private float acceleration;
        private float maxSpeed;
        private float time;

        void Start()
        {

            for (int j = 0; j < 20; j++)
            {

                carSpeedGraph.AddKey(j, 0.01f * j);

            }
            carSpeedGraph.AddKey(21, .5f);

        }


        void Update()
        {

            transform.position += new Vector3(carSpeed, 0, 0);

            carSpeed = carSpeedGraph.Evaluate(time);

            time += Time.deltaTime;

        }
    */

}
